# backend/.env

# Data API Base URL (마지막 슬래시 제외)
#DATA_API_HOST=http://[ITM-Data-API주소]:8081
DATA_API_HOST=http://10.172.111.88:8081

# [추가] 데모(개발) 모드 활성화 (true 시 AD 인증/토큰 검증 무시)
# 개발 중에는 true 로 설정하는 것을 권장. false 로 할 경우 아래 SAML 설정이 실제 유효한 값이어야 함.
ENABLE_DEMO_MODE=false
