# backend/.env
# ==========================================
# [ ITM.Vision_SSO Backend Environment ]
# ==========================================

# 1. 서버 포트 설정
# HTTPS 적용 및 프론트엔드/백엔드 통합 포트 사용을 위해 8082로 설정
PORT=8082

# 2. 데이터베이스 연결
# 사용하시는 실제 DB 연결 문자열로 유지하세요. (기본값: SQLite)
DATABASE_URL="file:./dev.db"

# ==========================================
# [ SAML 인증 설정 (Node.js 샘플 코드 매핑) ]
# ==========================================

# [필수] IDP 진입점 (Node.js 샘플: idpUrl - Ops)
# 사용자가 로그인할 때 이동할 AD 서버 주소입니다.
SAML_ENTRY_POINT=https://stsds.secsso.net/adfs/ls/

# [필수] Issuer / Entity ID (Node.js 샘플: issuer)
# AD에 등록된 이 서비스의 식별자입니다.
# 주의: 개발 PC IP나 포트가 바뀌어도, AD에 등록된 ID가 'https://localhost:44364'라면 이 값은 그대로 둬야 합니다.
SAML_ISSUER=https://localhost:44364

# [필수] Callback URL (Node.js 샘플: callbackUrl)
# AD 인증 성공 후 돌아올 주소입니다. (개발 PC 포트 8082 및 HTTPS 적용)
# 주의: AD 담당자에게 이 주소(https://localhost:8082/api/auth/callback)를 Reply URL로 등록 요청해야 합니다.
SAML_CALLBACK_URL=https://localhost:8082/api/auth/callback

# [필수] IDP 인증서 (Node.js 샘플: cert)
# [직접 입력] node.js_샘플코드.txt 파일 안의 'cert' 값('MII...'로 시작하는 긴 문자열)을
# 따옴표 없이 아래 등호(=) 뒤에 공백 없이 한 줄로 붙여넣으세요.
SAML_IDP_CERT=

# [선택] 로그아웃 URL
# AD 로그아웃 처리를 위한 URL입니다.
SAML_LOGOUT_URL=https://stsds.secsso.net/adfs/ls/?wa=wsignoutcleanup1.0

# [선택] SP 개인키
# AD 설정상 서명이 필요 없다면 비워두세요.
SAML_SP_PRIVATE_KEY=

# ==========================================
# [ 프론트엔드 연동 설정 ]
# ==========================================

# [필수] 로그인 성공/실패 후 리다이렉트될 Vue 프론트엔드 주소
# 개발 PC 환경(HTTPS 8082) 반영
FRONTEND_URL=https://localhost:8082/login
