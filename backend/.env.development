# backend/.env.development
# [개발 AD 환경 설정]
# 개발 메타데이터 XML 내 <SingleSignOnService>의 Location URL (보통 /adfs/ls/로 끝남)
# 예: entityID가 http://stsds-dev... 이면 보통 아래와 같음
SAML_ENTRY_POINT=https://stsds-dev.secsso.net/adfs/ls/

# AD에 등록한 이 시스템(SP)의 식별자 (개발 PC인 경우 localhost 유지)
SAML_ISSUER=https://localhost:44364

# 인증 후 돌아올 콜백 주소
SAML_CALLBACK_URL=https://localhost:44364/api/auth/callback

# [중요] 개발용 메타데이터 XML 안의 <X509Certificate> 값 (줄바꿈 없이 한 줄로)
SAML_IDP_CERT=MIIC... (개발용 인증서 값) ...

# 기타 설정
PORT=44364
DATABASE_URL="postgresql://postgres:pw@localhost:5432/itm?schema=public"
FRONTEND_URL=https://localhost:8082/login

# [필수] SP 개인키 (기존 .env에서 복사)
# AD에 보낼 요청(AuthnRequest)을 서명할 때 사용
SAML_SP_PRIVATE_KEY=

# [필수] SP 공개키 (기존 .env에서 복사)
# 메타데이터(/api/auth/metadata) 생성 시 포함되어 AD가 우리를 검증할 때 사용
SAML_SP_PUBLIC_CERT="-----BEGIN CERTIFICATE-----
... (기존 값) ...
-----END CERTIFICATE-----"
